{
	"private": true,
	"license": "MIT",
	"scripts": {
		"build": "turbo run build",
		"c": "pnpm --filter=@just-web/react-commands",
		"check:fix": "biome check --write",
		"check": "biome check",
		"clean": "turbo run clean",
		"coverage": "turbo run coverage",
		"cs": "changeset",
		"depcheck": "turbo run depcheck",
		"dev": "turbo run dev --parallel",
		"f": "pnpm --filter=react-freedom",
		"lint": "turbo run lint",
		"nuke:local": "rimraf node_modules",
		"nuke": "pnpm --no-bail --filter=* nuke && pnpm --no-bail nuke:local",
		"postinstall": "husky install",
		"r": "pnpm --filter=@just-web/react",
		"release": "pnpm build && changeset publish",
		"ri": "pnpm --filter=@just-web/react-intl",
		"sb": "turbo run sb --parallel",
		"size": "turbo run size",
		"test": "turbo run test",
		"update_snapshots": "turbo run update_snapshots",
		"verify:pkg": "turbo run lint build coverage depcheck size",
		"verify": "run-p check verify:pkg",
		"version": "changeset version"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.8",
		"@changesets/assemble-release-plan": "^6.0.9",
		"@changesets/cli": "^2.29.8",
		"@commitlint/cli": "^20.2.0",
		"@commitlint/config-conventional": "^20.2.0",
		"@repobuddy/biome": "^2.2.0",
		"husky": "^9.1.7",
		"npm-run-all": "^4.1.5",
		"playwright": "^1.57.0",
		"plop": "^3.1.1",
		"rimraf": "~5.0.5",
		"turbo": "1.10.13",
		"typescript": "^5.7.3"
	},
	"packageManager": "pnpm@10.25.0",
	"pnpm": {
		"overrides": {
			"@types/react": "^16.14.43"
		},
		"patchedDependencies": {
			"@changesets/assemble-release-plan@6.0.9": "patches/@changesets__assemble-release-plan@6.0.9.patch"
		}
	}
}
